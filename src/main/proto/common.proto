syntax = "proto3";

package dev.baseio.slackdata.common;

option java_multiple_files = true;

message SKByteArrayElement{
  int32 byte = 1;
}

message Empty {
  string nothing = 1;
}

// The types of public keys.
enum KeyAlgorithm {
  RSA_ECDSA = 0;
}